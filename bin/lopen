#!/usr/bin/env bash
source $(dirname $0)/../lib/functions.inc

### END FRONTMATTER ###
set -e

BROWSER="$(command -v firefox)"


case $(input_type "$1") in
  file) open_file "$1" ;;
  url)  open_url "$1" ;;
esac
